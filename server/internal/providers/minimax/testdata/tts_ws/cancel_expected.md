# MiniMax WebSocket TTS Cancel Fixture

On context cancel, close the WebSocket and drop any task result received after cancel. Before adapter code, re-confirm the WebSocket auth header against official docs.
